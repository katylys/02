version: "3"
services:
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
#  app:
#    build:
#      context: ./..
#      dockerfile: test/Dockerfile
#    ports:
#      - "4000:4000"
#    depends_on:
#      - mongo